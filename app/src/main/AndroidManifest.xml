<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.lahaptech.lahap">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity android:name=".user.activity.CartActivity"/>
        <activity android:name=".owner.UpdateProductActivity" />
        <activity android:name=".user.activity.DetailActivity" />
        <activity android:name=".owner.AddNewProductActivity" />
        <activity android:name=".owner.CheckOrderActivity" />
        <activity android:name=".owner.HomeOwnerActivity" />
        <activity android:name=".user.activity.HomeUserActivity" />
        <activity android:name=".auth.RegisterActivity" />
        <activity android:name=".auth.LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>